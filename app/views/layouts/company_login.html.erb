<!DOCTYPE html>
<html>
  <head>
    <title>Foome - Login de Empresa</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-white min-h-screen">
    <div class="flex min-h-screen">
      <!-- Lado esquerdo (formulário) -->
      <div class="flex-1 flex items-center justify-center px-6 py-12">
        <div class="max-w-md w-full">
          <%= link_to root_path, class: "block text-center mb-8" do %>
            <div class="flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#ff4b4b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-utensils-crossed">
                <path d="m16 2-2.3 2.3a3 3 0 0 0 0 4.2l1.8 1.8a3 3 0 0 0 4.2 0L22 8"/>
                <path d="M15 15 3.3 3.3a4.2 4.2 0 0 0 0 6l7.3 7.3c.7.7 2 .7 2.8 0L15 15Zm0 0 7 7"/>
                <path d="m2.1 21.8 6.4-6.3"/>
                <path d="m19 5-7 7"/>
              </svg>
              <span class="ml-2 text-2xl font-semibold text-[#ff4b4b]">foome</span>
            </div>
          <% end %>
          
          <% if flash[:notice] %>
            <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded mb-4">
              <%= flash[:notice] %>
            </div>
          <% end %>
          
          <% if flash[:alert] %>
            <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-4">
              <%= flash[:alert] %>
            </div>
          <% end %>
          
          <%= yield %>
        </div>
      </div>
      
      <!-- Lado direito (imagem) - aparece apenas em telas lg ou maiores -->
      <div class="hidden lg:flex lg:flex-1 bg-gray-100 items-center justify-center relative">
        <div class="absolute bottom-10 left-10 right-10 bg-white/80 backdrop-blur-sm p-6 rounded-lg">
          <p class="text-lg text-gray-800 italic">
            "Com o foome, conseguimos oferecer um benefício alimentar de qualidade para nossos funcionários, com uma rede diversificada de estabelecimentos."
          </p>
          <p class="mt-2 text-sm text-gray-600 font-medium">
            Maria Santos, Diretora de RH
          </p>
        </div>
        <div class="w-full h-full bg-gray-200 flex items-center justify-center">
          <svg xmlns="http://www.w3.org/2000/svg" width="150" height="150" viewBox="0 0 24 24" fill="none" stroke="#ff4b4b" stroke-width="1" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-utensils-crossed">
            <path d="m16 2-2.3 2.3a3 3 0 0 0 0 4.2l1.8 1.8a3 3 0 0 0 4.2 0L22 8"/>
            <path d="M15 15 3.3 3.3a4.2 4.2 0 0 0 0 6l7.3 7.3c.7.7 2 .7 2.8 0L15 15Zm0 0 7 7"/>
            <path d="m2.1 21.8 6.4-6.3"/>
            <path d="m19 5-7 7"/>
          </svg>
        </div>
      </div>
    </div>
  </body>
</html> 